/**
 * @module models
 */
/**
 * The values corresponding to guild ranks as they are received over the network.
 */
export enum GuildRank {
  NoRank = -1,
  Initiate = 0,
  Member = 10,
  Officer = 20,
  Leader = 30,
  Founder = 40
}
